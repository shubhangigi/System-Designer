services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: archspace
      POSTGRES_PASSWORD: archspace
      POSTGRES_DB: archspace
    ports:
      - "5433:5432"
    volumes:
      - archspace-postgres:/var/lib/postgresql/data

volumes:
  archspace-postgres:

